<h5>Listing Weights</h5>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Weight</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @weights.each do |weight| %>
      <tr>
        <td><%= weight.date_id %></td>
        <td><%= weight.weight %></td>
        <td><%= link_to 'Show', weight %></td>
        <td><%= link_to 'Destroy', weight, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
