<h2>Enter a new weigh-in</h2>

<%= form_for(@weight) do |f| %>


<% if @weight.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@weight.errors.count, "error") %> prohibited this record from being saved:</h2>

    <ul>
    <% @weight.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <div class="field">
    <%= f.label :weight %><br>
    <%= f.number_field :weight %>
  </div>
  <div class="field">
    <%= f.label :date_id %><br>
    <%= f.number_field :date_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', weights_path %>
